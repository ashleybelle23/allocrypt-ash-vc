import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Allocrypt — Deploy capital in one click',
  description:
    'Allocrypt makes DeFi easy for everyone. Deploy into curated yield opportunities on Monad and Ethereum. Fully non-custodial.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full`}>
      <body className="min-h-full flex flex-col antialiased bg-[#080b14] text-zinc-100">
        {children}
      </body>
    </html>
  );
}
